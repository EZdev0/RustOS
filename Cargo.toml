[workspace]
resolver = "2"
members = [
    "builder",
    "kernel",
]

[profile.release]
opt-level = 3
lto = "fat"
codegen-units = 1
panic = "abort"
strip = "debuginfo"

[profile.dev]
opt-level = 1
panic = "abort"
