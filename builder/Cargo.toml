[package]
name = "builder"
version = "0.2.0"
edition = "2021"

[dependencies]
# FIX: UEFI-Zwang deaktiviert (default-features = false). 
# Wir zwingen den Bootloader, exakt NUR das BIOS-Image zu bauen!
bootloader = { version = "0.11.7", default-features = false, features = ["bios"] }
